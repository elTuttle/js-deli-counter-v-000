var katzDeli = []

function takeANumber(katzDeliLine, name){
  var newArray = []
  var deliPlace = newArray.length + 1
  newArray.push(`Welcome, `+(name)+`. You are `+(deliPlace)+` in line` )
  return newArray
}
